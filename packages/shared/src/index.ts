// Placeholder for shared types
export interface User {
  id: string;
  email: string;
  name: string;
}

export interface Treasure {
  id: string;
  title: string;
  description: string;
  latitude: number;
  longitude: number;
}
